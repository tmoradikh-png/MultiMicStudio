# Production dependency set for hosted deployments.
#
# Includes everything in requirements.txt plus the extras a hosted backend needs:
#   - boto3: S3-compatible object storage (AWS S3 / Cloudflare R2 / DO Spaces / MinIO),
#     imported lazily by app/storage.py:S3Storage and only used when STORAGE_BACKEND=s3.
#
# psycopg2-binary (PostgreSQL driver) is already in requirements.txt.
#
#   pip install -r requirements-prod.txt

-r requirements.txt

boto3==1.35.24

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Data / DB ---
sqlalchemy==2.0.34
psycopg2-binary==2.9.9          # PostgreSQL driver (full product)
# MVP can fall back to SQLite (built into Python) via DATABASE_URL

# --- Validation / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth ---
bcrypt==4.2.0
pyjwt==2.9.0

# --- Audio processing ---
numpy==2.1.1
scipy==1.14.1                   # cross-correlation for sync
soundfile==0.12.1               # read/write WAV
# Prefer a system FFmpeg on PATH (faster, full codec support). If absent, this
# pip-bundled binary is used automatically so the app works out of the box.
imageio-ffmpeg==0.5.1

# --- Audio test bench (scripts/audio_bench.py: quality report + plots) ---
matplotlib==3.11.0              # waveform/spectrum/spectrogram plots
pyloudnorm==0.2.0               # LUFS loudness (optional; bench degrades without it)

# --- Transcription (optional, heavy) ---
# Install one of these to enable real transcription; otherwise a stub is used.
# faster-whisper==1.0.3
